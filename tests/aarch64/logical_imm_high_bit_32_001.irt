--TEST--
AArch64 32-bit high-bit logical immediate must not overflow
--TARGET--
aarch64
--ARGS--
-O1 --dump-size
--CODE--
{
	uint32_t c_1 = 2147483648;
	l_1 = START(l_4);
	uint32_t d_2 = PARAM(l_1, "x", 1);
	uint32_t d_3 = AND(d_2, c_1);
	l_4 = RETURN(l_1, d_3);
}
--EXPECT--
