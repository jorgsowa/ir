--TEST--
AArch64 O0 FP constant binary op must not assert
--TARGET--
aarch64
--ARGS--
-O0 --dump-size
--CODE--
{
	float c_1 = 1.0;
	l_1 = START(l_3);
	float d_2 = ADD(c_1, c_1);
	l_3 = RETURN(l_1, d_2);
}
--EXPECT--
