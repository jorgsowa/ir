--TEST--
AArch64 64-bit high-bit logical immediate must not overflow
--TARGET--
aarch64
--ARGS--
-O1 --dump-size
--CODE--
{
	uint64_t c_1 = 9223372036854775808;
	l_1 = START(l_4);
	uint64_t d_2 = PARAM(l_1, "x", 1);
	uint64_t d_3 = AND(d_2, c_1);
	l_4 = RETURN(l_1, d_3);
}
--EXPECT--
