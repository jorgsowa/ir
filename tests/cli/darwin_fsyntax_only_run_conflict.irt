--TEST--
Darwin fsyntax-only conflicts with run
--TARGET--
aarch64-darwin
--ARGS--
-fsyntax-only --run
--CODE--
{
	l_1 = START(l_2);
	l_2 = RETURN(l_1);
}
--EXPECT--
ERROR: -fsyntax-only is not compatible with native compilation flags (-S, --run, --dump-size)

exit code = 1
